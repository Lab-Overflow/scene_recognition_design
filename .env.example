# Reference template only.
# For one-click setup, run: npm run config:keys
# Keys will be saved to .env.local and loaded automatically by startup scripts.
# Choose one provider in command line: --provider openai | anthropic
OPENAI_API_KEY=your_openai_key
ANTHROPIC_API_KEY=your_anthropic_key
